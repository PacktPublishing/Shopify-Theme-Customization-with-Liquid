const addSelector = document.querySelectorAll(".collection-submit");
if (addSelector.length) {
  for (let i = 0; i < addSelector.length; i++) {
    addSelector[i].addEventListener(click, (e) => {
      e.preventDefault();
      addCart(this);
    });
  }
}